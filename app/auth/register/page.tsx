import { PageShell } from "@/components/page-shell";

export const metadata = { title: "Create Account" };

export default function RegisterPage() {
  return (
    <PageShell title="Create Account" subtitle="Registration ships in Phase 5.">
      <p>The customer registration form is wired up in <strong>Phase 5</strong>.</p>
    </PageShell>
  );
}
